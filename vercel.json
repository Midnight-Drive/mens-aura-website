{
  "framework": "vite",
  "buildCommand": "pnpm -r --filter @workspace/mens-aura run build",
  "outputDirectory": "artifacts/mens-aura/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
