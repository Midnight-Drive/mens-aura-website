{
  "framework": "vite",
  "buildCommand": "pnpm -r --filter @workspace/mens-aura run build",
  "outputDirectory": "artifacts/mens-aura/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
